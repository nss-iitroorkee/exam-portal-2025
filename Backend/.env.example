# Copy this file to `.env` and fill with real values before running `docker-compose up`.

# App
PORT=3000

# MongoDB - the docker-compose sets MONGODB_URI to mongodb://mongo:27017 by default.
# If you want to use an external DB, set the full URI here (without the DB name):
MONGODB_URI=mongodb://mongo:27017

# Database name (used by code constants)
DB_NAME=CrackItHub

# JWT secrets and expiries
ACCESS_TOKEN_SECRET=your_access_token_secret_here
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here
REFRESH_TOKEN_EXPIRY=7d

# Cloudinary (optional - used for uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Twilio (optional - used if sending SMS)
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
